services:
  database:
    image: mysql:8
    environment:
      MYSQL_DATABASE: app
      MYSQL_ROOT_PASSWORD: "!ChangeMe!"
      MYSQL_PASSWORD: "!ChangeMe!"
      MYSQL_USER: app
    ports:
      - "3306:3306"
    volumes:
      - database_data:/var/lib/mysql:rw

volumes:
  database_data: